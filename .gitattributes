# Text files are stored with LF in the repository; each checkout uses the platform's own line ending.
* text=auto

# Windows scripts need CRLF; shell scripts need LF.
*.bat text eol=crlf
*.cmd text eol=crlf
*.ps1 text eol=crlf
*.sh text eol=lf

# Binary files
*.png binary
*.zip binary
*.pdf binary
